<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: enforces HTTPS-only with phantom-os Caddy cert pinned via
         QR-payload SHA-256 (see PairCrypto.verifyCertPin). Cert pin is checked
         at Ktor-client level, not here, so this file just disables cleartext
         globally and lets the certificate pinner own the trust chain.
         Dev (192.168.x.x): allow cleartext for `dev-no-pin` setups when the
         operator runs phantom-os without TLS termination. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- LAN dev hosts. Allows http:// to RFC1918 ranges so the QR with
         `dev-no-pin` cert hint can connect without TLS termination. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.0.0/8</domain>
        <domain includeSubdomains="false">172.16.0.0/12</domain>
        <domain includeSubdomains="false">192.168.0.0/16</domain>
        <domain includeSubdomains="false">phantom.local</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
