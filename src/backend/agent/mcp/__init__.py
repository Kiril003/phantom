"""
MCP (Model Context Protocol) extension point — Phase 9.2.

Discovery + adapter machinery for dynamic tools. NO real servers wired by
default; operators add servers via `agent_mcp_servers` config.
"""
