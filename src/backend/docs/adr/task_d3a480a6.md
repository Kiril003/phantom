# Architectural Decision: перевір навантаження на процесор

- **Task ID:** d3a480a6-2aff-4c7b-8245-f1d010f5d96f
- **Date:** unknown

## Rationale
Використання вбудованої в ОС утиліти (наприклад, `top` або читання з `/proc/loadavg`) через `bash.run` є найбільш ефективним та легкодоступним методом для отримання даних про навантаження. Цей підхід не вимагає встановлення додаткових пакетів і мінімізує вплив на систему, що особливо важливо при високому системному тиску (SYSTEM_PRESSURE: RED).

## Required Capabilities
- bash.run
- notify.desktop
