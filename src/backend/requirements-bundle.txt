# PHANTOM OS — залежності ДЛЯ БАНДЛА (Ф6, AppImage).
#
# `requirements.txt` — це середовище розробника: там є чим ганяти тести і
# чим водити браузер. Тут — рівно те, що має поїхати користувачеві.
#
# Виміряно 29.08.2026 на робочому venv (6,0 ГБ) — саме тому цей файл існує:
#
#   nvidia/*      2,7 ГБ   CUDA-рантайм, притягнутий торчем
#   torch         1,2 ГБ   збірка 2.13.0+cu130
#   triton        691 МБ   компілятор ядер GPU
#   playwright    137 МБ   драйвер браузера для e2e
#   ────────────────────────────────────────────
#                ~4,7 ГБ   з шести — усе це в продукті не потрібне
#
# ЧОМУ CPU-ТОРЧ, А НЕ «ГЕТЬ ТОРЧ». Спокуса викинути його цілком велика:
# в коді застосунку `import torch` не зустрічається жодного разу поза
# `generate_embeddings.py` (окремий утилітарний скрипт, не частина app).
# Але це хибний слід: `memory/embedding_fn.py:104` бере
# `SentenceTransformerEmbeddingFunction`, а та тягне `sentence_transformers`
# ліниво, вже всередині chromadb. Тобто торч потрібен стратегічній пам'яті —
# просто через дві непрямі ланки, яких grep по застосунку не бачить.
# Рахунок після заміни: CPU-колесо замість cu130 + зникають nvidia/* і triton.
#
# Ембединги рахуються на CPU за замовчуванням (`build_embedding_function`,
# `device: str = "cpu"`), тож CUDA-збірка не давала нічого й на робочій машині.

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu

fastapi==0.111.1
uvicorn[standard]==0.30.6
websockets==13.0.1
python-multipart==0.0.9
pydantic==2.8.2
pydantic-settings==2.4.0
sqlalchemy==2.0.32
aiosqlite==0.20.0
alembic==1.13.2
# ПІН ПІТОНА ЖИВЕ ТУТ, а не в CLAUDE.md: chromadb тягне `chroma-hnswlib==0.7.6`
# (C++-прив'язка hnswlib), а та має колеса win_amd64 для cp37…cp311 і НЕ МАЄ
# для cp312. Отже Windows-сайдкар можна зібрати ЛИШЕ на Python 3.11 — на 3.12
# крос-платформний розв'язок валиться саме тут, і повідомлення говорить про
# chroma-hnswlib, а не про пітон, тож причину легко не впізнати.
#
# Заміряно 06.09.2026 на phantom-gate: `uv pip compile --python-platform
# x86_64-pc-windows-msvc` дає 154 пакети, 153 з готовим колесом; єдиний виняток
# `srt` (залежність vosk) — чистий пітон і ставиться з sdist. Тобто ЄДИНА
# версійна стіна під Windows — оця.
#
# На Linux обмеження немає: там cp312-колеса є, і `pick_python` у
# build_sidecar.sh свідомо надає перевагу 3.12. Хто підніматиме пітон бандла —
# спершу перевір це колесо, інакше Linux поїде, а Windows тихо перестане
# збиратись.
chromadb==0.5.5
sentence-transformers==3.0.1
google-genai>=1.0.0
ollama==0.3.1
faster-whisper==1.0.3
vosk==0.3.45
soundfile==0.14.0
supertonic==1.3.1
piper-tts==1.4.2
pyserial-asyncio==0.6
opencv-python-headless==4.10.0.84
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==43.0.0
segno==1.6.1
zeroconf>=0.148,<1.0
psutil==6.0.0
platformdirs==4.3.6
croniter==6.2.2
anyio==4.4.0
httpx==0.27.0
cachetools==7.0.6
PyYAML==6.0.2
shapely==2.0.5
markdown==3.7
reportlab==4.2.5
# Витяг тексту з документів для моделі (`tools/document_text.py`). До цього
# `read_file` віддавав моделі base64, і на питання «що в цьому договорі» вона
# переказувала документ, якого не бачила.
#
# Вага заміряна 03.09.2026, не оцінена:
#   pypdf      3 852 КБ   PDF
#   openpyxl   2 784 КБ   XLSX/XLSM (+ et_xmlfile 84 КБ)
#   striprtf      44 КБ   RTF
#   ─────────────────────
#             ~6,7 МБ
#
# python-docx НЕ береться навмисно: він тягне lxml, разом 14,9 МБ — більше за
# решту набору вдвічі, — а віддає посимвольно той самий текст, що обхід
# розмітки стандартною бібліотекою (звірено на документі з таблицею), ще й
# губить порядок документа. DOCX, PPTX, ODT і ODS розбираються zipfile+re.
pypdf==6.16.2
openpyxl==3.1.5
striprtf==0.0.33


# НЕ включено навмисно, і кожне — з причиною:
#   playwright        драйвер браузера для e2e; у продукті нікому не потрібен
#   pytest*           тестовий рантайм
# Моделі (Vosk, Piper, whisper-чекпойнти, ембединг-модель) у бандл не їдуть
# узагалі — вони не пакети, а ваговиті блоби, і про них окреме рішення.
# Ембединг-модель `intfloat/multilingual-e5-small` (493 МБ: safetensors 470,6
# + tokenizer 17,1 + sentencepiece 5,1) у бандл теж НЕ їде — ухвалено 03.09,
# тим самим правилом, що голосові моделі 29.08 і дорожні пакети: важкі блоби
# ставляться окремо й за згодою, а не роздувають пакунок із 724 МБ до 1,2 ГБ.
#
# ОБЕРЕЖНО, тут раніше стояла неправда — я її й написав. Було: «без неї
# пам'ять на чистій машині піде в мережу при першому ж записі, бо
# embedding_fn пробує local_files_only=True, ловить виняток і тягне модель».
# Це перестало бути правдою 29.08 (b09f979): тепер кидається
# EmbeddingModelMissing, і без явного PHANTOM_ALLOW_MODEL_DOWNLOAD=1 у мережу
# ніхто не йде. Коментар описував поведінку, якої вже не було, — саме той
# спосіб, яким документація починає брехати раніше за код.
#
# Справжня вада не в мережі, а в мовчанні: без моделі стратегічна пам'ять
# просто мертва, і всі виклики `_get_ef()` сидять під `except Exception`.
# Стан тепер видно у `/health.memory_model` (604bef9) — і той шлях мусить
# збігатися з тим, який виставляє `_phantom_entry.py`, інакше пам'ять
# скаже «не встановлена» поруч із моделлю, що лежить за два каталоги.
