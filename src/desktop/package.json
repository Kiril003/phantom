{
  "name": "aegis-film",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AEGIS Stratum 0 — the Film: PHANTOM's permanent optical membrane over the host OS",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "tauri": "tauri"
  },
  "dependencies": {
    "@fontsource/playfair-display": "^5.0.0",
    "@fontsource/space-grotesk": "^5.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
